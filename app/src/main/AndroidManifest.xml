<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <queries>

        <!-- Queries for Instagram -->
        <intent>
            <action android:name="android.intent.action.SEND" />

            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />

            <data android:mimeType="text/plain" />
        </intent>

        <package android:name="com.instagram.android" />

        <!-- Queries for Snapchat -->
        <intent>
            <action android:name="android.intent.action.SEND" />

            <data android:mimeType="image/*" />
        </intent>

        <package android:name="com.snapchat.android" />

        <!-- Add more queries for other apps or interactions as needed -->
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gossy"
        tools:targetApi="31">
        <activity
            android:name=".editAccountActivities.EditAccountActivity"
            android:exported="false" />
        <activity
            android:name=".webViewActivity.WebViewActivity"
            android:exported="false" />
        <activity
            android:name=".notificationSee.SeeCommentActivity"
            android:exported="false" />
        <activity
            android:name=".notificationSee.CommentActivity"
            android:exported="false" />
        <activity
            android:name=".notificationSee.SeeLikePostActivity"
            android:exported="false" />
        <activity
            android:name=".notificationSee.SeeThePollActivity"
            android:exported="false" />

        <service
            android:name=".notifications.FirebaseService"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter>
        </service>

        <activity
            android:name=".auth.FetchDataLoadingActivity"
            android:exported="false" />
        <activity
            android:name=".auth.OTPActivity"
            android:exported="false" />
        <activity
            android:name=".auth.userDataFragments.GenderChooseFragment"
            android:exported="false" />
        <activity
            android:name=".auth.PhoneAuthActivity"
            android:exported="false" />
        <activity
            android:name=".auth.UserDataActivity"
            android:exported="false" />
        <activity
            android:name=".auth.PermissionActivity"
            android:exported="false" />
        <activity
            android:name=".auth.WelcomeAccountActivity"
            android:exported="true"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>